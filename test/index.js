import assert from 'assert';
// import * as index from '../src/index';

describe('Instant React App', () => {
  // it('index should exist', () => {
  //   assert.equal(typeof index, 'function');
  // });
});
